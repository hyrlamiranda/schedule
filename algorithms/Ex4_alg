# Calcular imposto, comprou muita muamba
x.60 / 100

algoritmo "Creza04"
var
  preco, imposto: Real
inicio
      Escreva("Qual o preco do produto? US$")
      Leia(preco)
       imposto <- (preco * 60)/100
       Escreva("O imposto sera de US$", imposto )
fimalgoritmo
